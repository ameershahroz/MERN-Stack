//Arrays --data is group together

const names =["wasif", "waqas","naveed", "haseeb"]
// names[44] = "rafay"
// console.log(names[44])
names[names.length] = "xyz"
console.log(names.length)

names.push("something else")

names.pop();
names.pop();
names.pop();